# simulate.tcl — xsim run-control script
# Called via: xsim <snap> -tclbatch simulate.tcl
# Compile + elaborate are handled by the Makefile BEFORE xsim is launched.
# This script only controls the simulation run itself.

run all
quit
